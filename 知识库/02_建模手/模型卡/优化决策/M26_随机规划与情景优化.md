---
card_id: M26
card_type: model
title: 随机规划与情景优化
category: 优化决策
roles:
- modeler
- engineer
- judge
verified: true
version: '1.0'
---

# 随机规划与情景优化

## 适用任务

需求、价格、产量等随机参数下的决策。

## 使用前提

情景生成能代表不确定性；决策时序和信息可用性正确；概率权重合理。

## 建模要点

区分先验决策和情景后决策，禁止使用决策时不可知信息。进行样本外情景评估并报告期望、尾部和失败概率。

最低工作流：定义预测/决策对象和分析单位 → 建立简单基线 → 仅在训练数据拟合预处理 → 拟合候选模型 → 用与数据结构匹配的验证方案评估 → 完成误差、稳健性与失败样本分析。

## 最低验证要求

情景外测试、EVPI/VSS、情景数收敛、概率敏感性。

推荐记录：期望收益、分位收益、违约概率、VSS、EVPI。

## 常见失败模式

用同一批情景优化和评价；随机参数独立假设无依据；未来信息泄漏。

## 替代或对照模型

鲁棒优化、分布鲁棒、CVaR优化。

## 工程实现提示

分离训练情景与评估情景；保存随机种子与生成参数。

## 评委检查点

- 是否说明为什么该模型比简单基线更适合本题，而不只是“模型先进”。
- 是否逐条核验使用前提，并处理不满足的条件。
- 数据切分、特征处理、调参和阈值选择是否与最终测试隔离。
- 是否报告不确定性、失败情形和结论适用边界。
- 复杂模型若无稳定且有实际意义的增益，应退回简单模型。
