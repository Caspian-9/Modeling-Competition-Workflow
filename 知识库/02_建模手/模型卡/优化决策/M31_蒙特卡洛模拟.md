---
card_id: M31
card_type: model
title: 蒙特卡洛模拟
category: 优化决策
roles:
- modeler
- engineer
- judge
verified: true
version: '1.0'
---

# 蒙特卡洛模拟

## 适用任务

风险传播、概率估计、不确定性量化。

## 使用前提

输入分布及相关结构有依据；随机数生成正确；样本量满足误差要求。

## 建模要点

从联合分布采样并传播到输出。若变量相关，使用协方差、Copula或情景联合采样，不能默认独立。

最低工作流：定义预测/决策对象和分析单位 → 建立简单基线 → 仅在训练数据拟合预处理 → 拟合候选模型 → 用与数据结构匹配的验证方案评估 → 完成误差、稳健性与失败样本分析。

## 最低验证要求

蒙特卡洛标准误、样本量收敛、分布假设敏感性、极端值稳定性。

推荐记录：均值/分位数、概率、MCSE、置信区间。

## 常见失败模式

分布凭空指定；忽略相关性；样本量大却模型结构错误；只报均值。

## 替代或对照模型

Bootstrap、解析传播、拉丁超立方。

## 工程实现提示

固定种子但报告多种子验证；保存分布参数与采样代码。

## 评委检查点

- 是否说明为什么该模型比简单基线更适合本题，而不只是“模型先进”。
- 是否逐条核验使用前提，并处理不满足的条件。
- 数据切分、特征处理、调参和阈值选择是否与最终测试隔离。
- 是否报告不确定性、失败情形和结论适用边界。
- 复杂模型若无稳定且有实际意义的增益，应退回简单模型。
