---
card_id: M03
card_type: model
title: 广义线性模型GLM
category: 统计建模
roles:
- modeler
- engineer
- judge
verified: true
version: '1.0'
---

# 广义线性模型GLM

## 适用任务

计数、比例、二元和正偏响应建模。

## 使用前提

响应分布属于合适指数族；链接函数合理；均值-方差关系与数据相符。

## 建模要点

根据响应选择 Binomial/Poisson/Gamma 等分布和链接函数。计数模型必须检查暴露量并使用 offset。

最低工作流：定义预测/决策对象和分析单位 → 建立简单基线 → 仅在训练数据拟合预处理 → 拟合候选模型 → 用与数据结构匹配的验证方案评估 → 完成误差、稳健性与失败样本分析。

## 最低验证要求

过度离散、零膨胀、残差与影响点检查；比较Poisson和负二项；报告偏差与置信区间。

推荐记录：Deviance、AIC、对数似然、预测误差、覆盖率。

## 常见失败模式

对计数直接OLS；忘记offset；Poisson方差假设明显失效仍沿用；比例数据越界。

## 替代或对照模型

负二项、零膨胀、Beta回归、GAM。

## 工程实现提示

statsmodels family与link显式配置；预测时保留暴露量定义。

## 评委检查点

- 是否说明为什么该模型比简单基线更适合本题，而不只是“模型先进”。
- 是否逐条核验使用前提，并处理不满足的条件。
- 数据切分、特征处理、调参和阈值选择是否与最终测试隔离。
- 是否报告不确定性、失败情形和结论适用边界。
- 复杂模型若无稳定且有实际意义的增益，应退回简单模型。
