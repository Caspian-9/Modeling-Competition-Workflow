---
card_id: M39
card_type: model
title: Bootstrap与置信区间
category: 验证解释
roles:
- modeler
- engineer
- judge
verified: true
version: '1.0'
---

# Bootstrap与置信区间

## 适用任务

估计不确定性、模型性能区间、稳定性。

## 使用前提

重采样单位符合数据独立结构；重复数足够；统计量定义稳定。

## 建模要点

独立样本按行Bootstrap，分组/时间数据需按组或块重采样。区间可用百分位、BCa等方法。

最低工作流：定义预测/决策对象和分析单位 → 建立简单基线 → 仅在训练数据拟合预处理 → 拟合候选模型 → 用与数据结构匹配的验证方案评估 → 完成误差、稳健性与失败样本分析。

## 最低验证要求

检查区间随重复数稳定；明确训练重拟合还是仅重算指标；报告重采样单位。

推荐记录：标准误、置信区间、覆盖率。

## 常见失败模式

重复测量按行抽样；只对预测值重采样却声称包含训练不确定性。

## 替代或对照模型

解析CI、贝叶斯后验、重复交叉验证。

## 工程实现提示

固定主种子并派生子种子；保存抽样索引摘要。

## 评委检查点

- 是否说明为什么该模型比简单基线更适合本题，而不只是“模型先进”。
- 是否逐条核验使用前提，并处理不满足的条件。
- 数据切分、特征处理、调参和阈值选择是否与最终测试隔离。
- 是否报告不确定性、失败情形和结论适用边界。
- 复杂模型若无稳定且有实际意义的增益，应退回简单模型。
