---
card_id: M21
card_type: model
title: VAR与多变量时序
category: 时间序列
roles:
- modeler
- engineer
- judge
verified: true
version: '1.0'
---

# VAR与多变量时序

## 适用任务

多个内生序列联动预测、动态关系。

## 使用前提

序列平稳或协整得到处理；滞后阶合理；参数量相对样本可控。

## 建模要点

VAR让每个变量依赖所有变量的滞后；有协整时考虑VECM。脉冲响应描述模型内动态，不自动等同因果效应。

最低工作流：定义预测/决策对象和分析单位 → 建立简单基线 → 仅在训练数据拟合预处理 → 拟合候选模型 → 用与数据结构匹配的验证方案评估 → 完成误差、稳健性与失败样本分析。

## 最低验证要求

稳定性根、残差自相关、Granger预测性、滚动预测；滞后阶敏感性。

推荐记录：多变量预测误差、AIC/BIC、区间覆盖。

## 常见失败模式

样本少变量多；把Granger称为真正因果；未来外生变量使用真实值造成泄漏。

## 替代或对照模型

动态因子、VECM、状态空间、树模型。

## 工程实现提示

按时间切分；预测期外生变量必须有可用生成方式。

## 评委检查点

- 是否说明为什么该模型比简单基线更适合本题，而不只是“模型先进”。
- 是否逐条核验使用前提，并处理不满足的条件。
- 数据切分、特征处理、调参和阈值选择是否与最终测试隔离。
- 是否报告不确定性、失败情形和结论适用边界。
- 复杂模型若无稳定且有实际意义的增益，应退回简单模型。
