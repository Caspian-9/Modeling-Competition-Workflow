---
card_id: M12
card_type: model
title: 随机森林与ExtraTrees
category: 机器学习
roles:
- modeler
- engineer
- judge
verified: true
version: '1.0'
---

# 随机森林与ExtraTrees

## 适用任务

表格分类回归、非线性交互、稳健基线。

## 使用前提

样本近似同分布；树数充足；分类不平衡得到处理。

## 建模要点

Bootstrap样本与随机特征降低树间相关。ExtraTrees增加随机切分，通常更快但偏差可能更高。

最低工作流：定义预测/决策对象和分析单位 → 建立简单基线 → 仅在训练数据拟合预处理 → 拟合候选模型 → 用与数据结构匹配的验证方案评估 → 完成误差、稳健性与失败样本分析。

## 最低验证要求

OOB与交叉验证对照；排列重要性；分组/时间数据使用对应切分；检查概率校准。

推荐记录：ROC/PR-AUC、F1、MAE/RMSE、OOB误差。

## 常见失败模式

用不纯度重要性比较高基数变量；忽略时间顺序；无限制搜索后只报最好分数。

## 替代或对照模型

梯度提升、正则化线性模型、CatBoost。

## 工程实现提示

固定随机种子和n_jobs；保存特征名与训练版本。

## 评委检查点

- 是否说明为什么该模型比简单基线更适合本题，而不只是“模型先进”。
- 是否逐条核验使用前提，并处理不满足的条件。
- 数据切分、特征处理、调参和阈值选择是否与最终测试隔离。
- 是否报告不确定性、失败情形和结论适用边界。
- 复杂模型若无稳定且有实际意义的增益，应退回简单模型。
